pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/Premchand-96/static-website.git'
            }
        }

        stage('Deploy to Server') {
    steps {
        sh '''
        # 1. Clear out the directory
        sudo rm -rf /var/www/html/*
        
        # 2. Copy files (using full path to be safe)
        sudo cp -r ${WORKSPACE}/. /var/www/html/
        
        # 3. Fix Permissions
        sudo chown -R apache:apache /var/www/html
        sudo chmod -R 755 /var/www/html
        
        # 4. Restart to be sure
        sudo systemctl restart httpd
        '''
    }
}

        stage('Restart Apache') {
            steps {
                sh 'sudo systemctl restart httpd'
            }
        }
    }

    post {
        success {
            echo "Successfully deployed! Check http://13.53.40.0"
        }
        failure {
            echo "Deployment failed. Check the Jenkins Console Output for errors."
        }
    }
}
